# Verification Matrix

| Test | What to check |
|---|---|
| Front-door wiring (CLI↔core↔plugin) | A `commands/abcd/*.md` surface shells to the binary → `internal/surface/cli` parses input → the core returns a **structured result** the door renders. The same core call is reachable from the MCP door with no logic re-expressed (adr-23). Core behaviour with no front door calling it is dead scaffolding — fails the wiring check |
| ahoy idempotency | Run twice same version → no marker duplication, "already up to date", transparent re-confirm of visibility |
| ahoy upgrade | Bump plugin version → meta.json updated, marker content refreshed, no duplication |
| ahoy visibility flip | Switch private→public → `.gitignore` updated correctly, user shown resulting tracked-vs-ignored list |
| ahoy scanner probe | scanner seam defaults to the native secret/PII scan; a missing opt-in backend (gitleaks/Presidio; TruffleHog when scan.deep=true) → native scan used, opt-in install offered with confirm — never a hard-fail on absence |
| ahoy rules-loader install | Marker block from the bundled defaults injected; `.abcd/rules.json` skeleton written; the prompt-router hook registered |
| Probe report | `/abcd:disembark probe` on corpus reports correct source-reader findings per repo (rich on idelphiDev, sparse on idelphiSubZero) |
| Native transcript capture | A session transcript is written to the root-SHA-keyed native local store, **redacted at write time before anything lands on disk** (adr-29, reusing adr-6's two-stage model); the store is gitignored; an optional specstory import merges by timestamp/content hash over the same store |
| Native spec store | Directory-as-truth (adr-26): a spec's location encodes its status; the dependency graph resolves ready work; planning and sequencing run with no external tool. With the ccpm backend wired, the same on-disk shape round-trips at the convention level (adr-24) |
| Recency mechanic | Decision changed across specs → `spec-essence.json` body=latest, earlier in `superseded_by` |
| Pass B targeting | chat-distiller receives only the time-windowed subset from the native transcript store; `rationale-fills.json` cites transcript filenames |
| Agent dedup | chat-distiller does not re-emit pitfalls already in `.abcd/memory/pitfalls.md` |
| Agent context budget | Inputs over `disembark.maxAgentTokens` trigger stream + summarise; one-shot under threshold |
| Host-delegated oracle (default) | Oracle seam with **no adapter wired** → abcd emits the prompt, the host's subagent dispatch runs it, the structured result returns to the caller; never blocks, needs no API keys (adr-25) |
| Oracle adapter degrade | A wired oracle adapter (native/CLI/API/MCP) that is unreachable → degrades to the host-delegated default rather than failing the run |
| Oracle audit quality | `audit/oracle-<ts>.json` has specific findings, not vague approval |
| Oracle re-review | After a fix, the audit-fix loop re-dispatches the review prompt through the oracle seam; the verdict can downgrade (SHIP→NEEDS_WORK) and is honoured; each dispatch is independent, so the loop re-primes context every iteration |
| Disembark checkpoint | Ctrl-c mid-`disembark to <path>` writes a checkpoint to `.abcd/logbook/disembark/<ts>/_state.json` (forensic record). No `resume` sub-verb ships initially; re-running `disembark to <path>` starts fresh — checkpoint exists for post-mortem only. |
| Lifeboat re-run | Second run triggers transparent confirm; Overwrite creates `.bak` |
| Embark source lookup | 5 scenarios: (1) `embark from home` (current repo's lifeboat) → no prompt, (2) `embark scan` lists sibling lifeboats ranked by mtime, (3) `embark scan` with no candidates → empty result with hint, (4) `embark scan --deep` walks 3 levels, (5) `embark from <path>` with explicit path → no scan |
| Embark emptiness | Non-empty→refuse; `--force`→conflict-report.md; only .git/+LICENSE→succeed |
| Embark conflict bulk prompt | N conflicts → single transparent prompt with scope summary; resolution applied uniformly |
| Embark asset curation | keep=copied verbatim, adapt=user prompted with suggestion, drop=silent skip |
| Embark RP workspace registration | Lifeboat with `.abcd/rp/workspace.json` + the opt-in RP adapter present → user prompted to register; RP adapter absent → graceful warn-and-continue |
| Round-trip | disembark on idelphiDev → embark into empty target → new repo memory+ADRs faithful subset |
| Multi-repo corpus | All milestone acceptance gates pass on each corpus repo (with documented exemptions) |
| Launch preflight | Deliberate PII fixture → hard-fail with preflight.md path |
| Launch curated release (single repo) | Packaging **excludes `.abcd/**`** from the release artifact; a `/abcd:launch dry-run` proves nothing under `.abcd/` appears in the artifact manifest (adr-28). There is no second repo — the cut is a build-time filter over one tree |
| Launch manifest | Correct include/exclude; `.abcd/launch.allow` honoured |
| Launch branch mode | `--mode=branch` → `launch/<version>` branch; main untouched |
| Launch version stamp | Default patch bump; `--version` override; the version is stamped into the **curated release artifact** at cut time (the working tree stays unversioned — adr-19/adr-20 polarity on one tree); `marketplace.json` updated |
| Launch documentation-auditor | docs/ inspected; warn-fail surfaces user prompt before proceeding |
| Asset discovery | Prompt fires when assets found; user-supplied paths merged into curator input; `_provenance.json` records origin |
| Intent capture | `/abcd:intent "<text>"` (canonical bare quoted create, per fn-30/itd-46) runs interview → writes `intents/drafts/itd-N-<slug>.md` with press-release content + persona quote + acceptance criteria (per itd-1) |
| Intent plan | `/abcd:intent plan <itd-N>` → lints acceptance criteria → **native plan handoff** scaffolds the bidirectionally-linked spec in the native spec store (ccpm backend when wired) → injects bidirectional link → moves drafts/→planned/ |
| Intent acceptance lint | `/abcd:intent plan` on a draft missing `## Acceptance Criteria` → hard block, error message names the missing section |
| Intent ship | `/abcd:intent ship <itd-N>` → **native spec work handoff** (or full pipeline if intent in drafts/); on spec completion the lifecycle hook auto-moves planned/→shipped/ |
| Intent lifecycle hook | native spec marked done → linked intent auto-moves `planned/` → `shipped/` and a review is **queued** on that transition (shipped by fn-28; fn-28 also backfilled already-shipped intents that never had a review queued). Auto-running `intent-fidelity-reviewer` off that queue is still **deferred** — no spec currently owns it; fn-6 disowned auto-firing. fn-12 ships the manual `/abcd:intent review` surface |
| Intent help | bare `/abcd:intent` shows intents grouped by state with suggested next actions; same pattern across all abcd commands |
| Intent fidelity review (Role 1) | fn-12 ships the **discipline-judgement subset** — per-criterion verdicts (MET/MET_WITH_CONCERNS/NOT_MET/INCONCLUSIVE) for itd-1 acceptance and the itd-37 `MG004` boilerplate check; the broader three-bucket prose audit (honoured / diverged / missing) is **deferred** to a later spec. Manual run via `/abcd:intent review <itd-N>` works |
| Persona registry | The persona registry (`internal/core/personas`) picks persona from `.abcd/development/personas.json`; role-hint biasing works; never reuses real names |
| Bidirectional link drift | `internal/core/lint` warns when intent's `spec_id` and spec's `intent` field disagree; runs as pre-commit hook (and in CI) |
| Press-release oracle audit | Pass C `press-release-composer` runs the oracle-seam product audit; findings appended to press-release.md as "Open Questions" |
| Embark press-release interview | First embark step shows lifeboat press release + audit; user confirms/amends; amended version becomes embark contract |
| Capture write (fn-21) | `/abcd:capture "<text>"` → file at `.abcd/development/activity/issues/open/iss-N-<slug>.md` with frontmatter populated; stdout reports `captured iss-N <slug> (severity: <sev>)`; defaults applied per fn-21 (severity=`minor`, category=`observation`, source=`user-observation`, found_during=`manual-capture`); slug derived from text unless `--slug` overrides |
| Capture resolve (fn-21) | `/abcd:capture resolve iss-N "<note>"` → file moves `open/`→`resolved/` with `resolution` + `updated` fields populated; stdout reports `resolved iss-N → resolved/`; `UnknownIssueId` → exit 3, `TransitionConflict` → exit 4 |
| Capture wontfix (fn-21) | `/abcd:capture wontfix iss-N "<reason>"` → file moves `open/`→`wontfix/` with `wontfix_reason` + `updated` fields populated; stdout reports `wontfix iss-N → wontfix/ (reason recorded)` |
| Capture bare render (fn-21) | Bare `/abcd:capture` → read-only status render (counts `open N · resolved N · wontfix N`, up to 10 most-recent open issues, next-action hints); no `iss-*.md` created/moved/mutated. fn-20 safety preamble (txn recovery, orphan cleanup, ledger-dir ensure) is allowed |
| Capture filtered list (fn-21) | `/abcd:capture list --open` / `--resolved` / `--wontfix` / `--all` → one line per issue (`iss-N <state> <severity> <slug>`), sorted numerically by N. Unfiltered CLI `abcd capture list` (no flag) → exit 2 with "choose a filter" message |
| Capture promote (fn-30, itd-46 — LIVE) | `/abcd:capture promote iss-N` → reads the issue, runs the agent-driven intent-new interview seeded with the issue body, writes the four-field back-link (`source_issue`/`promoted_to` scalars + `related_issues`/`related_intents` lists). Skill-orchestrated (lock-acquire → promote-inspect → seed+interview → promote-link → release); never a `capture promote` CLI sub-verb. Guarded by the `PR000`/`PR001`/`PR002` lint family |
| Capture migration (fn-22) | First `dev-sync work` after upgrade → `.work/issues.md` entries promoted to structured ledger with provenance noting "migrated from .work/issues.md" |
| Rules-loader keyword recall | Prompt containing "commit" / "PR" → COMMITTING domain rules injected; unrelated prompt → zero abcd rules injected (token overhead < 200) |
| Rules-loader dedup | Same prompt three times → rules inject on prompt 1, skip on 2-3 (until forced refresh on prompt 5) |
| Rules-loader star-command | `*ROADMAP draft a milestone` → ROADMAP rules inject regardless of keyword match |
| Host dispatch (no adapter) | An oracle call with no adapter wired → abcd emits the prompt; the host's subagent dispatch runs it; the verdict-tagged result returns to the caller |
| Host dispatch independence | Two consecutive host-delegated audit calls → independent dispatches (no cross-call continuity); the audit-fix loop re-primes context every iteration |
| RP workspace pull | `dev-sync rp` (opt-in RP adapter) → `.abcd/rp/workspace.json` exists; `~/`-relative paths in source rewritten to `~/`-form |
| RP workspace embark | Lifeboat with `.abcd/rp/workspace.json` + opt-in RP adapter present → user prompted to register; written to `~/Library/Application Support/RepoPrompt/Workspaces/` |
| Voyage embark provenance | `embark from <path>` → `.abcd/development/voyage/embark/provenance.json` exists with source path + manifest hash + timestamp + files written |
| Voyage embark archive | `embark from <path> --archive` → input lifeboat copied verbatim to `.abcd/development/voyage/embark/from/<timestamp>/`; path referenced from `provenance.json` |
| Voyage disembark history | Each disembark run → new line appended to `.abcd/development/voyage/disembark/history.jsonl` with `manifest_sha256`, files, oracle backend, verdict |
| Voyage no snapshot proliferation | Re-running disembark → `.abcd/lifeboat/` overwritten with `.bak` safety net; no `lifeboat-v1/` / `lifeboat-v2/` directories; previous manifest preserved in `disembark/history.jsonl` |
| Grill Phase 1 — Socratic interview (itd-27) | `/abcd:intent grill <itd-N>` runs Phase 1 interactive Socratic interview; emits `grill-report.{json,md}` at `.abcd/logbook/grill/<utc-ts>-<itd-N>/`; every question tagged with one of six named Socratic moves (Definition / Elenchus / Dialectic / Maieutics / Counterfactual / Generalization); once `terminology/` exists, glossary-aware mode flags GL002 (forbidden synonym) and GL003 (cross-context collision) and offers inline glossary edits; lite mode (`--lite`) caps at 3 questions/round, 12/session without glossary load |
| Grill Phase 2 — PRD synthesis (itd-27) | After Phase 1 completes (or `--done` flag), Phase 2 runs silently: synthesises Pocock-shaped PRD (Problem Statement / Solution / User Stories ≥5 / Implementation Decisions / Testing Decisions / Out of Scope / Further Notes) from sharpened intent + grill findings + glossary citations + ADRs in scope; writes to `.abcd/intents/<itd-N>/prd.md` with frontmatter (`intent`, `grill_session_id`, `source_intent_hash`, `grill_report_hash`); Phase 2 cannot be skipped |
| Grill PRD freeze and provenance (itd-27) | `/abcd:intent plan <itd-N>` validates PRD exists (GR002 blocker if absent) → writes `frozen_at`, `frozen_content_hash` (SHA-256 of PRD body + stable frontmatter excluding `{frozen_at, frozen_content_hash, spec, planning_attempt_id}`; provenance fields `source_intent_hash`, `grill_report_hash`, `grill_report_path` are INCLUDED), `planning_attempt_id` (UUIDv4) to PRD frontmatter → any subsequent body mutation triggers GR003 blocker; re-compute hash matches frozen hash (roundtrip pass); mutated body hash mismatches (roundtrip fail → GR003) |
| Grill planner handoff (itd-27) | `/abcd:intent plan` runs the native plan handoff with the frozen PRD as primary context + press-release as citation; the resulting spec `## Links` YAML block (Option A convention) contains `intent: itd-N`, `prd: .abcd/intents/itd-N/prd.md`, AND `planning_attempt_id: <uuid>` keys |
| Grill glossary lint codes (itd-27) | `GL001` undefined term (warn); `GL002` forbidden synonym (blocker); `GL003` cross-context collision without `contexts:` (warn); `GL004` non-stable term (draft or deprecated status) in promoted intent (blocker); `GL005` missing glossary citation — body uses recognised term but `glossary_terms_used` omits it (blocker for promoted, warn for draft); `GR001` intent never grilled (warn); `GR002` planned/ intent missing PRD (blocker); `GR003` frozen PRD modified (blocker); `GR004` stale planning attempt — `planning_attempt_id` set but no matching spec Links block AND >24 h since `frozen_at` (warn); `GR005` provenance mismatch — `source_intent_hash` OR `grill_report_hash` does not match on-disk file (blocker) |
| Grill injection resistance (itd-27) | Phase 1: adversarial intent body containing "ignore previous instructions" does NOT produce `exit_reason: natural_completion` without running questions; Phase 2: synthesised PRD `## Implementation Decisions` does not contain attacker-controlled text (manual smoke; CI replay lands in a later phase as itd-6) |
| Grill determinism (itd-27) | Same intent + same flags → same Phase 1 question-set hash; `--fresh` rotates move priority → deterministically different hash; same intent + same Phase 1 transcript → identical Phase 2 PRD (hashable); fixtures in `tests/fixtures/grill/determinism-baseline.json` and `determinism-prd-baseline.md` (CI enforcement lands in a later phase via the itd-6 replay harness) |
| Spec-tied reviews (itd-28) | Review artefacts land in the native spec review store (`<spec-id>/<ts>/`) with index entry, oracle backend recorded, and re-review re-dispatches through the oracle seam |
| Three intent kinds (itd-34) | Frontmatter `kind` field is binding (set at plan time); multi-arg `/abcd:intent plan <itd-A> <itd-B>` creates a shared spec with both intents in `spec.intent` and matching `bundle:` field on each member; `/abcd:intent reclassify --kind <new-kind>` records `reclassification_history`; supersession path writes both `superseded_by` and `kind_at_supersession`; disciplines in `disciplines/` carry no `status` field (lint blocks) |
| Prompt linter (itd-5) | Every `agents/*.md` has `prompt_version: <semver>` (per itd-5 Add 1); linter rejects missing field |
| Prompt self-improvement preflight | Each agent's `1.0.0` lock recorded in `agents/CHANGELOG.md` (per itd-5 Add 2) |
| Injection-canary fixture | Each scope-relevant agent (chat-distiller, embark-scaffolder, issue-scout, code-rescuer, decision-archaeologist, review-collator) has at least one canary fixture that the agent passes (per itd-5 Add 3) |
| Memory unification (itd-36) | `/abcd:memory ingest <pdf>` produces typed entity/topic page(s) with `source.class: external_pdf` + citation frontmatter; original NOT stored at `.abcd/memory/sources/`; ingest log entry appended; `index.md` regenerated |
| Memory keep-original (itd-36) | `/abcd:memory ingest <pdf> --keep-original` stores original at `.abcd/memory/sources/<sha256>.<ext>`; the lifeboat licence gate (`/abcd:disembark`, NOT launch — adr-18) refuses publish without an explicit allowlist entry. `/abcd:launch` excludes `.abcd/**` wholesale and never publishes `.abcd/memory/sources/`; at launch the gate is future/inert (dry-run renders its verdicts diagnostically) |
| Memory quotation lints (itd-36) | Per-page `MQ001` fires on >150-word contiguous quotation OR >5% per-page coverage; cumulative `MQ002` fires on >25% deduplicated source coverage across N pages citing same `source_hash` |
| Memory source-class lints (itd-36) | `MS001` (advisory) fires on single-source-class synthesis; `MS002` blocks on cross-class synthesis without `weighting_note`; `ML001` blocks on `external_*` page missing `licence` field |
| Memory schema-extension on existing (itd-36) | After itd-36 ships, existing flat-named `<type>_<domain>_<slug>.md` files are NOT renamed; `index.md` generated over them; `source.class: session_memory` backfilled as default |
| Memory bare = render (itd-36) | Bare `/abcd:memory` shows status (page count by class, last-ingest timestamp, recent contradictions, quotation budget headroom); never mutates state |
| Provenance substrate | Source-hash registry at `.abcd/memory/.sources_index.json` updates on every ingest; SPDX licence extracted from declared sources OR explicit `unknown`; cross-consumer (memory + future loot) uses one registry |
| Modification grammar capture (itd-37) | Every spec carries `## Modification Grammar` section with three sub-headings (`Extends cleanly` / `Breaks the design` / `Why`) plus the `Ripple` axis (vocabulary / surface / coupling deltas); concreteness lint `MG001`-`MG003` fires on missing section / sub-heading / concrete reference |
| Modification grammar boilerplate detection (itd-37) | `intent-fidelity-reviewer` Role 1 (discipline role) fires `MG004` on `## Modification Grammar` text that could equally describe a different spec (strip-the-name test) — semantic LLM judgement, not regex |
| Modification grammar extraction (itd-37) | At spec completion, `principle-distiller` writes `spec_modification_grammar_<spec_id>.md` (append-only) AND updates curator-merged `modification_grammar_<domain>.md` (compounding-curated) under `.abcd/memory/` |
| Vocabulary registration (itd-37 Ripple) | Every term introduced in a spec's `### Ripple > Vocabulary delta` MUST appear in `02-constraints/04-naming.md` glossary in the same spec; `internal/core/lint` blocks at plan-review with `VR001` on missing registration |
| Recovery humility (idea-2) | `/abcd:disembark` and `/abcd:embark` surface docs carry the recovery-humility paragraph (≤4 sentences); embark closing imperative names "hunt the originating session before trusting the lifeboat blindly" |
| Capability scope (idea-4, itd-5 extension) | Every agent prompt frontmatter carries `capability_scope` with `task_classes` (closed-enum tokens) + `designed_for` (free-text 1-line); `internal/core/lint` validates set-membership at plan-review |
| Oracle seam contract preserved (idea-4) | Adding `capability_scope` does NOT modify the oracle seam (host-delegated by default, opt-in adapters — adr-25); the later-phase capability-aware routing (Frontier Awareness) is a pre-dispatch selector above the seam, not a modification to the seam contract |
| Surface discipline (sweep) | No `/abcd:<verb> show`, `<verb> stats`, `<verb> list` (plain), or `<verb> view` sub-verbs ship; bare invocation renders state for all `/abcd:<verb>` commands; lint `SD001` blocks on forbidden sub-verb introduction; earned exception preserved — each of `/abcd:capture list --open`, `/abcd:capture list --resolved`, `/abcd:capture list --wontfix`, `/abcd:capture list --all` (flag immediately adjacent to `list`, never pipe-joined into one token) |
