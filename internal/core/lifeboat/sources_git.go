package lifeboat

// gitSources returns the Tier-0 adapters: every brief section derivable from
// git history alone (present in any git repository). Filled by M2's git adapter.
func gitSources() []Source {
	return nil
}
