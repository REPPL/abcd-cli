# CLI reference (generated)

**Generated build output — do not hand-edit.** These pages are produced from the
Cobra command tree (`abcd` doc generation) and regenerated in CI; hand edits are
overwritten and fail the freshness check. To change this content, change the command
definitions in `internal/surface/cli/` and regenerate.
